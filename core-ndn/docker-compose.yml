version: "3"
services:
  core-ndn:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /lib/modules:/lib/modules
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ./dotcore:/root/.coregui
      - ../wasm_rest:/root/wasm_rest
      - ../resources:/root/resources
      - ../core-test-setups:/root/tests
      - ../log:/root/log
    privileged: true
    cap_add:
      - NET_ADMIN
      - SYS_ADMIN
    environment:
      - DISPLAY
      #- BACKEND
